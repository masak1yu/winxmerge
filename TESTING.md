# TESTING.md — リリース前手動テストチェックリスト

このチェックリストは、リリースごとに**コピーして**使う。過去のPR（#64, #66, #67, #69, #70, #72）はいずれも「GUIの手動検証はしていません」という未チェックの確認項目付きでマージされており、その項目はPR本文にしか残らないため次のリリースには一切引き継がれない。このファイルはその置き場所として、リポジトリに永続する形でチェック項目を保持する。

## 使い方

- リリース作業を始める前に、このファイルをコピーして日付とタグ名を入れたファイル名（例: `testing-v0.51.0.md`）で作業する。このファイル自体は編集しない。
- 各項目は「1操作 + 1期待結果」。書かれている操作をそのまま行い、書かれている結果にならなければ fail としてIssueを起票する。
- 前提となるファイルや状態は、各項目の中に明記してある。特記のない限りテストデータは `testdata/` 以下のファイルを使う。
- OSに依存する項目は「（macOS）」「（Windows）」「（Linux）」を付けている。付いていない項目は3OS共通で確認する。

---

## 1. 準備（ビルド、テスト用ファイルの用意）

このリリースで配布する3プラットフォームのバイナリが実際にビルド・起動できることと、以降の章で使うテストデータが揃っていることを確認する。ここが失敗した場合、以降の章は着手しない。

- [ ] `cargo build --features desktop` を実行し、警告なく成功し `target/debug/winxmerge`（Windows: `target/debug/winxmerge.exe`）が生成される
- [ ] `cargo test --features desktop` を実行し、全テストが成功する（`--features desktop` を付けないとテストが1件も見つからず0件成功で終わるため、必ず件数が0でないことも確認する）
- [ ] `cargo fmt --check` を実行し、差分（diff）が出力されない
- [ ] `cargo clippy --features desktop -- -D warnings` を実行し、警告件数が直前のリリース時点から増えていない（`main` ブランチでの既知件数と比較する）
- [ ] 既存の設定ファイル（macOS/Linux: `~/.config/winxmerge/settings.json`、Windows: `%APPDATA%\winxmerge\settings.json`）を別名でバックアップしてから削除し、次の起動で既定値のウィンドウが例外なく開く
- [ ] `testdata/` に存在しない以下のファイルをこのリリースのテスト用に用意する: 2シート以上を持つ `.xlsx` を2つ（`left.xlsx`/`right.xlsx` など、片方のセル値だけ変更したもの）、NULバイトを含むバイナリファイルを2つ（先頭8KB以内にNULが必要 — 例: 小さな画像や実行ファイルの断片）、通常の `.zip` アーカイブを2つ（中身のファイル数が違うもの。拡張子は `.zip` で `.xlsx`/`.xlsm`/`.ods` ではないこと）
- [ ] （Windows）配布用ビルド `cargo build --release --features embed-icon,desktop` を実行し、生成された `winxmerge.exe` のアイコンがWindowsエクスプローラー上で既定のexeアイコンではなくアプリアイコンになっている
- [ ] （macOS）`./scripts/build-macos-bundle.sh` を実行し、`WinXMerge.app` が生成されてFinderから起動できる

---

## 2. 起動と基本操作（新規比較、タブ、終了時の保存確認）

複数タブをまたいだ基本操作と、Help→Keyboard Shortcutsダイアログに書かれている内容が実際のキー割り当てと一致しているかを確認する。ここは他章より先に、実際に動くショートカットの一覧を確定させる役割も持つ。

- [ ] File → New(N) → Text で空の2-way比較タブが開き、両ペインとも空の1行だけの編集可能なテキストになっている
- [ ] File → New(N) → Table でダイアログが出て、確定すると10列×5行の空グリッドの表形式タブが開く
- [ ] File → New (3-pane)(N) → Text で空の3-way比較タブ（ベース/左/右の3ペイン）が開く
- [ ] File → New (3-pane)(N) → Table でダイアログが出て、確定すると3ペインの空グリッドが開く
- [ ] File → Open... で `testdata/left.txt` と `testdata/right.txt` を選ぶと2-way比較タブが開く
- [ ] File → Open Left File... / Open Right File... をそれぞれ実行し、両方選び終えた時点で自動的に2-way比較が開始される
- [ ] File → Open Left Folder... / Open Right Folder... で `testdata/dir_left` / `testdata/dir_right` を選ぶとフォルダ比較タブが開く
- [ ] 直近で開いたファイル/フォルダのペアが File → Recent Files or Folders(I) のサブメニューに「左パス ↔ 右パス」の形式で表示され、クリックすると同じ組み合わせで再度開く
- [ ] Cmd/Ctrl+T で新規タブが追加され、Cmd/Ctrl+W でアクティブなタブが閉じる。タブが1つしかない状態でCmd/Ctrl+Wを押してもアプリ全体は終了しない
- [ ] 複数タブを開いた状態でタブを切り替えると、各タブのスクロール位置・カーソル位置・現在の差分インデックスがタブごとに独立して保持されている
- [ ] `testdata/left.txt` を編集して未保存の状態でウィンドウを閉じようとすると、保存確認ダイアログ（Save As キュー）が出て、保存/破棄/キャンセルを選べる。キャンセルするとウィンドウは閉じない
- [ ] 未保存の変更があるタブが複数ある状態でアプリを終了しようとすると、タブの数だけ保存確認が連続して出るか、まとめて一覧できる形で全タブ分の確認が行われ、どのタブの変更も確認なしに破棄されない
- [ ] Help → Keyboard Shortcuts を開き、ダイアログが表示される。次に、ダイアログに記載されている次の項目を実際に押して、記載どおりに動くか一つずつ確認する: Ctrl/Cmd+O、Ctrl/Cmd+Shift+S、Ctrl/Cmd+Home、Ctrl/Cmd+End、Ctrl/Cmd+Tab、Ctrl/Cmd+Shift+Tab、Ctrl/Cmd+→、Ctrl/Cmd+←、F7、F8、Ctrl/Cmd+D、Ctrl/Cmd++、Ctrl/Cmd+-、Ctrl/Cmd+Y（Redo表記）。**期待結果: これらは実際には割り当てられていないため、押しても何も起きないか、ブラウザ/OS標準動作以外の反応がない** — ダイアログの記載と実際の挙動が食い違っていることそのものを記録する
- [ ] 同じダイアログで「最初の差分」「最後の差分」の割り当て表記を確認する。**期待結果: ダイアログはCtrl/Cmd+Home・Ctrl/Cmd+Endと書いているが、実際に効くのはAlt+Home・Alt+Endである**
- [ ] 2-way比較タブで両ペインに未保存の変更を作り、Cmd/Ctrl+Sを1回押す。**期待結果: 左ファイルだけが保存され、右ファイルの未保存マークは消えない**（ダイアログは「右ファイルを保存」と書いているため、表記と逆になる）
- [ ] View → Toolbar のチェックを外すとツールバーが非表示になり、再度チェックすると復帰する
- [ ] View → Line Numbers のチェックを切り替えると、行番号列の表示・非表示が切り替わる
- [ ] View → Location Pane のチェックを切り替えると、ミニマップ状の差分概観ペインの表示・非表示が切り替わる
- [ ] View → Zoom In / Zoom Out を数回ずつ押すとペインのフォントサイズが段階的に変化し、Reset Zoom で初期サイズに戻る

---

## 3. ファイル比較（2-way）

2ファイルのテキスト差分表示・ナビゲーション・コピー系操作が、単純な追加/削除/変更のそれぞれで期待どおり動くことを確認する。

- [ ] `testdata/left.txt` と `testdata/right.txt` を開き、差分行が追加=緑・削除=赤・変更=黄など色分けされて表示される
- [ ] View → Word Diff をONにすると、変更行の中で実際に異なる単語だけが強調表示され、行全体ではなく単語単位のハイライトになる
- [ ] View → Diff Only をONにすると、差分のない行が畳まれて非表示になり、差分のある行の前後だけが表示される
- [ ] View → Diff Detail Pane をONにすると、現在選択中の差分の詳細（変更前後のテキストなど）を表示する専用ペインが開く
- [ ] View → Wrap Lines をONにすると、長い行がペイン幅で折り返して表示され、水平スクロールバーが消える
- [ ] Merge → First Difference / Previous Difference / Next Difference / Last Difference をそれぞれクリックし、現在の差分がその通りに移動する（3件以上差分があるファイルペアで確認する）
- [ ] Alt+↓（次）/ Alt+↑（前）/ Alt+Home（最初）/ Alt+End（最後）のキー操作でも同様に差分間を移動できる
- [ ] ツールバーの先頭/前/次/末尾ボタン（Merge menuと同じ4操作）でも同様に移動できる
- [ ] Merge → Copy Left to Right を実行すると、現在の差分の左側の内容が右側にコピーされ、その行が差分でなくなる
- [ ] Merge → Copy Right to Left でも逆方向に同様の結果になる
- [ ] ツールバーの「コピー＆次へ」ボタン（→側／←側）を押すと、コピー実行後に自動で次の差分に移動する
- [ ] ツールバーの「全差分をコピー」ボタン（左→右、右→左それぞれ）を押すと、そのファイルペアの全差分が一括でコピーされ、以降の比較で差分が0件になる
- [ ] Edit → Copy All Left Text / Copy All Right Text を実行し、それぞれ左ファイル全体・右ファイル全体のテキストがクリップボードにコピーされる（別のテキストエディタに貼り付けて確認する）
- [ ] 左ペインでテキストを選択した状態でEdit → Copy Selection →を実行すると、選択範囲だけが右ペインの対応する位置にコピーされる。← Copy Selectionでも逆方向に同様に動く
- [ ] クリップボードに任意のテキストをコピーした状態でEdit → Use Clipboard as Left を実行すると、左ペインの内容がクリップボードの内容に置き換わり、その内容と右ファイルとの比較になる。Use Clipboard as Right でも同様
- [ ] 拡張子が `.rs` など対応言語のファイルペアを開くと、キーワードや文字列リテラルなどがシンタックスハイライトされる。デスクトップ版でのみ有効な機能であることを確認する（WASM版では色分けされない — 16章参照）
- [ ] 片方のファイルで数行を上下に入れ替えた（移動した）テキストペアを比較し、移動元・移動先の行が青系のハイライトで示される。フォルダ比較のリストと違い、この移動行検出はトグルできるオプションである（12章参照）

---

## 4. 3-way マージ

ベース・左・右の3ペイン比較でコンフリクトが正しく検出され、L/R選択・自動マージ・個別保存が期待どおり動くことを確認する。

- [ ] `testdata/base.txt` `testdata/left_3way.txt` `testdata/right_3way.txt` を3-way比較で開くと、ベース/左/右の3ペインが並んで表示される
- [ ] 左右どちらか一方だけが変更した行は、コンフリクトではなく通常の差分として色分けされる
- [ ] 左右両方が同じ内容に変更した行は、赤（コンフリクト）にならず自動的にオートマージされ、結果側にその内容が反映される
- [ ] 左右が異なる内容に変更した行はコンフリクトとして赤でハイライトされる
- [ ] コンフリクトブロックで「L」ボタン（ツールバー左コピー、または対応するUI操作）をクリックすると左側の内容が採用される。「R」ボタンでは右側が採用される
- [ ] 「コピー＆次へ」に相当する操作（Use Left/Right and advance）でコンフリクトを解決すると、自動的に次のコンフリクトへ移動する
- [ ] 「全て左を採用」「全て右を採用」に相当する一括操作を実行すると、開いている全コンフリクトが一括で解決される
- [ ] Merge → Previous/Next Difference に相当するコンフリクト間移動（前/次のコンフリクトボタン）で、コンフリクトが複数あるファイルでコンフリクトだけを飛び移動できる
- [ ] 任意のペイン（ベース/左/右いずれも）で直接テキストを編集できる。編集後F5でRescanすると、編集後の内容から差分が再計算される
- [ ] 全コンフリクトを解決した後、File → Save Left / Save Middle / Save Right をそれぞれ個別に実行し、対応するファイルだけが更新される（Save Middle は3-way比較のときだけ有効になっている）
- [ ] コマンドラインから `winxmerge testdata/base.txt testdata/left_3way.txt testdata/right_3way.txt` を実行すると起動直後から3-way比較が開く
- [ ] `git mergetool`（15章のgit連携セットアップ後）でコンフリクトのあるマージを実行すると、winxmergeが3-way表示で起動し、保存して終了するとGitがマージ結果を取り込む

---

## 5. フォルダ比較

2つのフォルダ配下のファイル一覧を突き合わせ、比較方式の切り替えとフィルタ系オプションが実装どおりに（＝UIから到達できる範囲で）動くことを確認する。

- [ ] `testdata/dir_left` と `testdata/dir_right` をフォルダ比較で開くと、両フォルダの内容がファイル名ベースで突き合わされ、一致/相違/片側のみのステータスが一覧表示される
- [ ] 一覧上で相違ありのファイルをダブルクリックすると、そのファイルの詳細な2-way比較（またはバイナリならHex比較）が新しいタブで開く
- [ ] Edit → Options... → Folder で比較方式（Full Contents / Quick Contents / Binary Contents / Modified Date / Modified Date and Size / File Size / Existence）を切り替えてApplyすると、フォルダ比較の一覧の一致/相違判定がその方式に応じて変わる（例: File Sizeを選ぶと内容が違ってもサイズが同じなら一致扱いになる）
- [ ] 上記で選んだ比較方式が、アプリを再起動しても保持されている（settings.jsonのfolder_compare_methodに保存されるため）
- [ ] `winxmerge /m Size testdata/dir_left testdata/dir_right` を実行するとFile Size方式でフォルダ比較が開き、Options → Folderの選択欄がFile Sizeになっている。**この起動直後の状態ではアプリ終了時にfolder_compare_methodの設定値も書き換わる**（CLIの/mは設定を上書きするため、settings.jsonの値が変わっていることも確認する）
- [ ] Options → Folder の Exclude patterns にワイルドカードパターンを入力してApplyすると、一致するファイル/フォルダがフォルダ比較の一覧から除外される
- [ ] Options → Folder の Max depth に0以外の値を入れると、その階層数を超えるサブフォルダの中身が一覧に出なくなる
- [ ] Options → Folder の Min size / Max size にバイト数を入れると、範囲外のサイズのファイルが一覧から除外される
- [ ] Options → Folder の Modified after / Modified before に日付を入れると、条件に合わないファイルが一覧から除外される
- [ ] Edit → Options... の設定画面全体を確認する。**期待結果: `.gitignore` を自動で尊重するチェックボックスや、拡張子でフィルタするフィールドはOptionsダイアログのどこにも存在しない。** コマンドラインオプション一覧（14章）にも該当するものがないことを確認する
- [ ] フォルダ比較の一覧でファイルを右クリックし、Compare as Hex を実行する。両側に同名ファイルが存在する行でのみ有効になっており、実行するとそのペアがHex比較タブで開く
- [ ] 2つの`.zip`ファイル（準備章で用意したもの）を2-way比較で開く。**期待結果: Hexやテキストではなく、フォルダ比較と同じ形式のアーカイブ内エントリ一覧が開き、ステータスバーに `[ZIP]` の表示が出る**
- [ ] 準備章で用意した2つの `.xlsx` ファイルを2-way比較で開く。**期待結果: 上記のZIPアーカイブ一覧ではなく、6章のExcelテーブルビューが開く**（`.xlsx`/`.xlsm`/`.ods`はZIPコンテナだが、Excel判定がZIP判定より先に行われるため）
- [ ] File → Export Folder Report... を実行する（フォルダ比較タブでのみ有効）と、フォルダ比較結果のHTMLレポートが生成され、ブラウザで開くと一覧内容が表示される

---

## 6. 表形式の比較（CSV / Excel）

CSV/TSVのセル単位比較・編集・保存と、Excel系フォーマットの読み取り専用比較が期待どおり動くことを確認する。特に保存時の区切り文字の扱いはドキュメントの記載と実装が食い違っている点なので、明示的に確認する。

- [ ] `testdata/left.csv` と `testdata/right.csv` を開くと、セル単位で比較されたテーブルビューが開き、変更のあるセルだけがハイライトされる
- [ ] `testdata/left.tsv` と `testdata/right.tsv` を開くと、タブ区切りとして正しく認識されテーブルビューが開く
- [ ] `.csv` と `.tsv` を組み合わせて開くと、区切り文字が異なる旨の警告が表示される
- [ ] カンマや改行を含む値がダブルクォートで囲まれたCSVを開き、そのフィールドが1セルとして正しく分割される（区切り文字やセル内改行で誤って列がずれない）
- [ ] 列ヘッダーの境界をドラッグすると、その列の幅が変わる
- [ ] セルをクリックして値を編集すると、そのセルが編集済みとしてハイライトされ、Cmd/Ctrl+Zで元に戻せる。Cmd/Ctrl+Shift+Zでやり直しできる
- [ ] `testdata/left.tsv` を開いて任意のセルを編集し、Cmd/Ctrl+Sで保存する。保存後のファイルをテキストエディタで開き、**区切り文字がカンマに変換されず、タブ区切りのまま保存されている**ことを確認する（ドキュメントは「Cmd+SはCSVとして保存する」と書いているが、実際は開いたファイルの区切り文字を保持する）
- [ ] 新規作成したTable比較（保存先未指定）でCmd/Ctrl+Sを押すと、Save Asダイアログが表示される
- [ ] セルを編集した状態でF5(Rescan)を実行すると、編集済みの内容から差分が再計算され、未保存の編集内容が失われない
- [ ] File → New(N) → Table で空のテーブルを作ると、10列×5行の空グリッドになっている
- [ ] File → New (3-pane)(N) → Table で3ペインのテーブル比較（CSV 3-way）を作成し、`base.csv`/`left.csv`/`right.csv` を読み込ませた状態でコンフリクトのあるセルに対しツールバーの「左を採用」「右を採用」操作を行うと、そのセルの内容が置き換わる
- [ ] 準備章で用意した2シート以上ある `.xlsx` ファイル2つを比較すると、シートを切り替えるセレクターが表示され、シートごとに個別の差分結果になる
- [ ] Excel比較ビューで、セルをクリックしても編集モードに入らない（読み取り専用であることを確認する）。File → Save Left等も無効（グレーアウト）になっている
- [ ] `.xls`（旧形式）と `.ods`（OpenDocument）のサンプルファイルがあれば、それぞれ `.xlsx` と同様にテーブルビューで開けることを確認する（サンプルがない場合はスキップし、その旨を結果に記録する）
- [ ] Excel比較で変更セルがハイライトされ、追加/削除ではなく値の変更であることが色や記号で分かる

---

## 7. 画像比較

画像の差分検出・許容値スライダー・オーバーレイ表示がタブをまたいでも正しく独立して動くことを確認する。

- [ ] `testdata/image_left.png` と `testdata/image_right.png` を開くと画像比較ビューが開き、差分のあるピクセル領域が赤くハイライトされた差分パネルが表示される
- [ ] 許容値スライダーを0以外の値までドラッグして離すと、その時点で再計算が走り、差の最大値が許容値以下のピクセルは差分パネル上で黄色になる（赤ではなくなる）
- [ ] スライダーをドラッグしている最中は再計算が走らず、指を離した（マウスボタンを離した）瞬間にだけ再計算されることを、明らかに重い/遅延がある動きの有無で確認する
- [ ] 完全に同一のピクセルを持つ領域は、差分パネル上で灰色（減光表示）になる
- [ ] `testdata/image_small.png` のようにサイズが異なる画像同士を比較すると、はみ出した領域は許容値の設定に関わらず常に「変更」（赤）として扱われる
- [ ] Blendのオーバーレイ表示をONにすると2枚の画像が重ねて表示され、許容範囲内のピクセルは透明（重ねた背景がそのまま見える）、許容範囲外は赤系で重なって見える
- [ ] Zoomを操作して拡大/縮小しても、その後スライダーを操作して再計算した際にZoomの値がリセットされない
- [ ] 画像比較タブを2つ開き、それぞれで異なる許容値を設定してからタブを切り替えると、Blendのオーバーレイ画像と許容値スライダーの表示値がタブごとに正しく切り替わる（片方のタブの差分が他方に重なって表示されない）
- [ ] 許容値はタブ単位でのみ保持され、アプリを再起動すると既定値（0）に戻る。コマンドラインオプションにも許容値を指定するものがないことを14章の一覧と突き合わせて確認する
- [ ] 完全に一致する `testdata/image_same.png` を使ったペアで `winxmerge /enableexitcode` を実行する。**期待結果（既知の未対応）: 終了コードが画像比較の結果を反映せず、ウィンドウを手動で閉じるまでプロセスが終了しない、または閉じたときの終了コードが実際の比較結果と無関係な値になる** — 14章のCLI章でも同じ内容を確認する

---

## 8. Hex 比較

先頭8KB以内にNULバイトを含むバイナリファイルの比較がHexビューで開き、読み取り専用として安全に扱われることを確認する。以下はPR #70で挙げられていた未検証項目をそのまま引き継いでいる。

- [ ] バイナリのペア（準備章で用意したNULバイトを含むファイル2つ）を開くとHexビューになり、違うバイトに色が付く。前後移動でブロック単位（連続する差分バイトのまとまり）に移動し、現在のブロックの先頭行が強調される
- [ ] サイズの違うバイナリファイルペアで、短い方のファイルの終端より後ろの領域がすべて差分として表示される
- [ ] ウィンドウを狭くして横にスクロールしたとき、ヘッダー行（Offset / 左右のタイトル）が列に追従してスクロールする
- [ ] 画像ファイル（`testdata/image_left.png`など）をFile → Recompare As → Hexで開くと通常のHexビューになり、Normalに戻すと画像比較ビューに戻る
- [ ] フォルダ比較の一覧から右クリック→Compare as HexでHexビューが開き、Backボタンでフォルダ比較の一覧に戻れる
- [ ] `winxmerge /t Binary testdata/left.txt testdata/right.txt` を実行すると、通常はテキストと判定されるファイルでも強制的にHexビューで開く。さらに`/x`を付けると、内容が完全一致する2つのバイナリファイルを渡した場合に自動終了する
- [ ] Hexタブとテキスト比較タブを交互に切り替えても、それぞれのタブのスクロール位置・現在行が保持される
- [ ] バイナリのペアをHexビューで開いた状態でCmd/Ctrl+Sを押しても左ファイルの内容（ハッシュ値）が変わらない。次にテキスト比較タブで1文字編集してからCmd/Ctrl+Sを押すと、そのテキストファイルは正しく保存される
- [ ] 数十MB程度のサイズのファイル同士をHex比較で開いても、スクロール操作が目に見えて重くならない
- [ ] macOS/Windows/Linuxそれぞれの既定の等幅フォントで、Hexダンプ側とASCII側のセルの中に文字が収まりきらず欠けたり重なったりしていないことを確認する（Hex側22px相当、ASCII側9px相当のセル幅を目安にする）
- [ ] Hexビューでファイルの外部変更が自動検知されないこと（F5を押すまで再読み込みされないこと）を確認する

---

## 9. プロジェクトファイル

`.WinMerge` プロジェクトファイルの保存・読み込みが、WinMerge本体が作成したファイルも含めて壊れずに扱えることを確認する。以下はPR #72で挙げられていた未検証項目をそのまま引き継いでいる。

- [ ] 2-way、3-way、フォルダ比較のそれぞれのタブでFile → Save Project...を実行してプロジェクトファイルを保存し、アプリを再起動してFile → Open Project...でそのファイルを開くと、同じパスの組み合わせと同じ比較オプション（Ignore Whitespace等のタブ単位設定）で開く
- [ ] WinMerge本体で作成した `.WinMerge` ファイル（`filter` 要素や `*-readonly` 要素を含むもの）があれば開いてみて、エラーにならず既知の要素だけが反映されることを確認する（サンプルがない場合は本章の「読み書きする要素」の項目を手で1つずつXMLに書いて代用し、その旨を結果に記録する）
- [ ] `<paths>` エントリを複数持つプロジェクトファイルを開くと、エントリの数だけタブが開く
- [ ] コマンドライン `winxmerge <保存したプロジェクトファイル>.WinMerge` で起動すると、プロジェクトファイルの内容どおりに比較が開く（位置引数がプロジェクトファイル1つだけのときに限り認識されることも、2つ目の引数を追加して通常の2-wayとして扱われることで確認する）
- [ ] （Linux）ファイルを開くダイアログ（GTKのファイルブラウザ）からプロジェクトファイルを選んで開ける
- [ ] 中身を壊した（閉じタグを削除するなど）不正なXMLのプロジェクトファイルを開いても、アプリがクラッシュせず、ステータスバーにエラーメッセージが表示される
- [ ] プロジェクトファイルを開いた状態で編集し、終了時の保存確認（Save Asキュー、2章参照）がプロジェクト経由で開いたタブでも通常どおり機能する
- [ ] 準備章で用意した `.xlsx` 同士の比較が、プロジェクトファイル経由で開いてもExcelのテーブルビューで開く（ZIP一覧に吸われないことの再確認）
- [ ] `subfolders="0"`（探索を最上位のみに制限）を指定したプロジェクトファイルを開いた後、同じセッション内で別のフォルダ比較タブを新規に開く。**期待結果: 新しいフォルダ比較も最上位のみの探索になっている**（プロジェクトのsubfolders設定はアプリ全体のフォルダ比較設定を書き換えるため）。アプリを終了して再起動すると、settings.jsonのフォルダ比較設定にもこの制限が保存されている

---

## 10. インライン編集と保存

差分ペイン内でのテキスト編集がゴースト行の整合性を保ったまま行え、Rescanしても編集内容が失われないことを確認する。あわせて、Issue #63（比較時に無視した行が保存で消える）に対する v0.51 の暫定対応——行が落ちうる設定では保存そのものをブロックする——が意図どおり効いていることを確認する。

- [ ] 2-way比較のいずれかのペインで行頭にカーソルを置きEnterを押すと、その位置に新しい行が挿入され、反対側のペインには整列を保つためのゴースト行が自動的に追加される
- [ ] ゴースト行に対して直接テキストを入力すると、そのゴースト行は通常の行として扱われるようになる（is_ghostがfalseになり、以後の保存対象に含まれる）
- [ ] 行頭にカーソルがある空行でBackspaceを押すと、その空行が削除される
- [ ] Arrow Up / Down でペイン内の行間をカーソル移動できる
- [ ] 実在するファイルの内容と異なるファイル（外部で書き換えた）を、未保存の編集がある状態のタブで開いたままにしておく。**期待結果: 自動再スキャンはファイルの再読み込みをスキップし、編集内容を上書きしない**
- [ ] 上記と同じ状態でF5を手動で押すと、現在エディタに表示されている（未保存の）編集内容から差分が再計算される（ディスク上のファイルではなく、画面上の内容が基準になる）
- [ ] 1行しか残っていない状態のペインで、その最後の行を削除する操作を行う。**期待結果: 削除は無効化され、そのペインが編集不能な空の状態にならない**
- [ ] Cmd/Ctrl+Zで直前の編集を取り消し、Cmd/Ctrl+Shift+Zでやり直せる。取り消し・やり直しを連続で複数回実行しても内容が破綻しない
- [ ] File → New(N) → Text で作った空の2-way文書に数行入力し、Save Asで保存すると、新規作成した内容がそのままファイルに書き出される
- [ ] Edit → Options... → Filters で「空行を無視」をONにする。行数の異なる空行を含む2つのテキストファイル（例: 左に空行が1行多い `testdata/left.txt` の複製を作り空行を足したもの）を開いて2-way比較し、Cmd/Ctrl+Sで左ファイルを保存しようとする。**期待結果: 保存は実行されず、ステータスバーに `Save blocked: turn off "Ignore blank lines" and clear line filters first ...(#63)` が表示される。** 保存しようとしたファイルをテキストエディタで開き、ディスク上の内容が一切変わっていない（無視された空行がそのまま残っている）ことと、未保存マークが消えていないことを確認する
- [ ] 行フィルタ（Options → Filters の行フィルタ欄）に何か入力した状態でも、同じくCmd/Ctrl+Sでの保存がブロックされ、同じメッセージが出ることを確認する
- [ ] 続けて「空行を無視」をOFFに戻し行フィルタ欄も空にしてから、もう一度Cmd/Ctrl+Sを押す。**期待結果: 今度は保存が通り、空行を含む元の内容がそのまま書き出される**（ブロックはトグルを戻せば解除される可逆な仕組みであることの確認）
- [ ] 「空行を無視」をONにしたまま未保存の編集があるタブを開き、アプリを終了する。**期待結果: そのタブは保存されずに終了し、未保存の編集は失われるが、ディスク上の元ファイルからは行が消えていない。** 終了前にステータスバーへ `Save blocked for "<タブ名>": ...` が表示される。これは #63 の暫定対応が意図的に選んだトレードオフ（アプリ内に閉じる損失を許容し、ディスク上のファイル破壊を防ぐ）であり、v0.52 の根本修正で解消される
- [ ] 上記と同じ「空行を無視」設定のまま、`testdata/base.txt` / `testdata/left_3way.txt` / `testdata/right_3way.txt` で3-way比較を開く。**期待結果: 「空行を無視」の設定は3-way比較には一切反映されず、空行を含めた通常の差分・コンフリクト判定になる**（2-wayの設定値を共有するUIのチェック状態がONのままでも、3-wayの比較結果には影響しない）

---

## 11. 検索・置換・ブックマーク

差分ペイン内でのテキスト検索・置換と、ブックマークによる行移動が2-way・3-way双方で機能することを確認する。

- [ ] Cmd/Ctrl+Fで検索バーが開き、もう一度押すと閉じる
- [ ] 検索語を入力すると、マッチ数が検索欄の横に表示される
- [ ] 前へ/次へボタンで、マッチした位置の間をジャンプできる
- [ ] 3-way比較タブでもCmd/Ctrl+Fで同様に検索バーが使え、いずれかのペインを対象に検索できる
- [ ] 検索バーの置換欄に文字列を入れてReplaceを実行すると、現在ハイライトされているマッチだけが置換される
- [ ] Replace Allを実行すると、フォーカスのあるペインの全マッチが一括で置換される
- [ ] Cmd/Ctrl+Gで「行へジャンプ」ダイアログが開き、行番号を入力して確定するとその行にスクロール・フォーカスする
- [ ] Cmd/Ctrl+Mで現在の差分行のブックマークが切り替わる（トグル）。Navigate → Toggle Bookmark でも同じ操作ができる
- [ ] F2を押すと次のブックマーク行に移動する。Navigate → Previous Bookmark に対応するキーボードショートカットは存在しない（メニューからのみ実行できる）ことを確認する
- [ ] Navigate → Next Added / Prev Added / Next Removed / Prev Removed / Next Modified / Prev Modified をそれぞれ実行し、その種別の差分だけを飛び移動できる（追加行・削除行・変更行が両方あるファイルペアで確認する）

---

## 12. フィルタと差分オプション

行フィルタ・置換フィルタと5種類の差分オプションが、ツールバー・メニュー・Optionsダイアログ・CLIのどこから到達できるかを含めて確認する。ここには保存がブロックされる暫定対応（Issue #63）と、3-wayでの非対応範囲を含む。

- [ ] Edit → Options... → Filters の行フィルタ欄に `TODO|FIXME` のようなパイプ区切りの正規表現を入力してApplyすると、その正規表現にマッチする行が差分計算から除外される（該当行が差分としてもEqualとしても表示されず、行自体が比較対象から消える）
- [ ] Filters の置換フィルタ（Find/Replaceのペア）を1つ追加してApplyすると、指定した正規表現にマッチする箇所が比較時だけ置換後の文字列として扱われ、差分が変わる。保存後のファイルの中身は元のテキストのままで、置換フィルタの影響を受けていないことを確認する（行フィルタ欄は空にし「空行を無視」もOFFにしてから試す。どちらかが有効だと10章のガードで保存自体がブロックされるため）
- [ ] ツールバーを見て、Ignore Whitespace・Ignore Caseの2つのトグルボタンだけが存在することを確認する。**期待結果: 「空行を無視」「改行コードを無視」「移動行の検出」の3つはツールバー上にボタンがなく、Edit → Options...またはView/Merge相当のメニューからのみ切り替えられる**（ドキュメントはツールバーに5種類あると書いているが実際は2種類のみ）
- [ ] View → Ignore Whitespace / Ignore Case のチェック状態が、ツールバーの同名ボタンの状態と常に一致している（どちらから切り替えても他方に反映される）
- [ ] `/ignorews:1`（空白の変更を無視）と `/ignorews:2`（空白をすべて無視）をそれぞれ指定して起動し、Options上のWhitespaceの設定が「Ignore whitespace change」「Ignore all whitespace」のそれぞれ正しいモードになっていることを確認する
- [ ] Ignore Blank Lines をONにして起動し、行の追加/削除だけでなく空行の有無が異なるファイルペアで、空行の違いが差分として表示されなくなることを確認する
- [ ] Ignore EOL（改行コードを無視）をONにして、改行コードだけが異なる（CRLF/LF）2つのファイルを比較すると差分なしになる
- [ ] 移動行検出のトグルをOFFにすると、3章で確認した移動行の青ハイライトが出なくなり、単純な追加+削除として表示される
- [ ] ここまでのフィルタ・差分オプションの設定値が、アプリを再起動しても保持されている（settings.jsonに保存されるため）
- [ ] 10章の該当項目（空行無視・行フィルタが有効なとき2-wayテキストの保存がブロックされる件。Issue #63 の暫定対応）と、3-wayでは空行無視・行フィルタが完全に無効である件を、本章の観点からも再確認する（後者は既知の制限として結果に記録する）

---

## 13. エクスポートと印刷

差分結果のレポート出力と印刷が、対応するファイル形式・ビューの組み合わせで正しく生成されることを確認する。

- [ ] 差分のある2-way比較タブでFile → Print...を実行すると、システムの印刷ダイアログが開き、プレビューに差分の色分けが反映されている
- [ ] Hex比較タブ（view-mode 9）ではFile → Print...がグレーアウトして実行できない
- [ ] 差分が0件のタブ（完全に一致するファイルペア）ではFile → Print...がグレーアウトしている
- [ ] File → Export HTML Report... で生成したHTMLファイルをブラウザで開くと、色分けされた差分と、その差分に付けたコメント（あれば）が埋め込まれている
- [ ] File → Export Patch (Unified Diff)... で生成したファイルが `git apply --check` または `patch --dry-run` で妥当なユニファイド形式として認識される
- [ ] File → Export CSV... / Export TSV... を通常の2-wayテキスト比較タブで実行し、それぞれカンマ区切り・タブ区切りの差分一覧ファイルが生成される
- [ ] File → Export Excel (.xlsx)... で生成したファイルをExcelまたはLibreOffice等で開くと、追加=緑・削除=赤・変更=黄で行が色分けされ、コメント列がある
- [ ] File → Export Folder Report... はフォルダ比較タブ（view-mode 1）でのみ有効になっている
- [ ] 複数のタブにまたがって差分コメントを付けた状態で、File → Export All Comments (CSV)... を実行すると、全タブ分のコメントが1つのCSVに集約される
- [ ] 同様にExport All Comments (JSON)... を実行すると、JSON形式で機械可読な形で出力される
- [ ] 差分行に対してコメント（Note）を付けた状態でExport HTML Report...を実行すると、そのコメントの文言がHTML内に含まれている
- [ ] Export系メニューのうちExport CSV/TSV/Excel/HTML/Patchは、差分が1件もないタブ（diff_count=0）ではグレーアウトして実行できない

---

## 14. コマンドラインオプション

`src/cli.rs` の `USAGE` に記載されている全オプションを、実際にコマンドラインから渡して動作を確認する。git difftool/mergetoolやCIから直接利用される経路のため、GUI操作なしで再現できることが重要になる。

- [ ] `winxmerge /?` と `winxmerge --help` と `winxmerge -h` をそれぞれ実行し、いずれもUSAGE文をstdoutに出力してウィンドウを開かずに終了する
- [ ] `winxmerge testdata/left.txt testdata/right.txt` （オプションなし）で2-way比較が開く
- [ ] `winxmerge testdata/base.txt testdata/left_3way.txt testdata/right_3way.txt` で3-way比較が開く
- [ ] `winxmerge testdata/dir_left testdata/dir_right` （フォルダを2つ）でフォルダ比較が開く
- [ ] `winxmerge /ignorews:0 testdata/left.txt testdata/right.txt` （明示的に無効化）でIgnore Whitespaceがオフの状態で開く
- [ ] `winxmerge /ignorecase testdata/left.txt testdata/right.txt` でIgnore Caseが有効な状態で開く
- [ ] `winxmerge /ignoreblanklines testdata/left.txt testdata/right.txt` でIgnore Blank Linesが有効な状態で開く
- [ ] `winxmerge /ignoreeol testdata/left.txt testdata/right.txt` でIgnore EOLが有効な状態で開く
- [ ] `winxmerge /m Existence testdata/dir_left testdata/dir_right` と `winxmerge /m:size testdata/dir_left testdata/dir_right` の両方の書式（スペース区切り・コロン区切り）で、それぞれ指定した比較方式が適用される。大文字小文字を変えても（例: `EXISTENCE`）同じ結果になる
- [ ] `winxmerge /t Binary testdata/left.txt testdata/right.txt` と `winxmerge /t:binary testdata/left.txt testdata/right.txt` の両方でHexビューが開く。`/t Text` では自動判定に戻り、`/t Foo` のような未知の値では警告を出しつつオプションを無視して起動する
- [ ] `winxmerge /dl "Mine" /dm "Base" /dr "Theirs" testdata/base.txt testdata/left_3way.txt testdata/right_3way.txt` を実行すると、各ペインのヘッダーに指定した表示名が出る。新しくタブを開くとそのタブはパス表示に戻る（タブごとの一時的な上書きであることを確認する）
- [ ] `winxmerge /l 5 testdata/left.txt testdata/right.txt` を実行すると、起動直後にカーソルが5行目にジャンプした状態で表示される
- [ ] `winxmerge /e testdata/left.txt testdata/right.txt` で起動し、いずれのペインのテキストにもフォーカスがない状態でEscを押すとウィンドウが閉じる。テキスト入力欄にフォーカスがある状態でEscを押しても閉じないことも確認する
- [ ] 完全に一致する2ファイルで `winxmerge /x testdata/left.txt testdata/left.txt` を実行すると、比較後に自動的にウィンドウが閉じてプロセスが終了する。内容が異なるペアでは自動終了しない
- [ ] `winxmerge /xq` でも `/x` と同じ（自動終了する）動作になることを確認する
- [ ] `winxmerge /enableexitcode testdata/left.txt testdata/left.txt`（同一内容）を実行して手動でウィンドウを閉じ、終了コード（`echo $?`、PowerShellなら `$LASTEXITCODE`）が0になる。内容が異なるペアでは1になる。存在しないパスを渡した場合は2になる
- [ ] `winxmerge /enableexitcode` を画像ファイルのペア（準備章で用意したもの、内容を変えたもの）に対して実行し、手動でウィンドウを閉じて終了コードを確認する。**期待結果（既知の未対応）: 画像比較では比較結果が終了コードに反映されず、実際には内容が異なっていても終了コードが0のままになる**（7章の該当項目と合わせて確認する）
- [ ] `winxmerge --clear-history` を実行すると、セッション情報と最近使ったファイル/フォルダの一覧がクリアされてプロセスが終了する。直後にGUIを起動し、2章で確認したRecent Files or Foldersのサブメニューが空になっていることを確認する
- [ ] `winxmerge --ignore-whitespace testdata/left.txt testdata/right.txt`（`-w`でも可）、`--ignore-case`（`-i`でも可）、`--ignore-blank-lines`（`-B`でも可）のロング/ショート両形式が、それぞれ対応する`/ignore*`オプションと同じ結果になる
- [ ] `/`で始まる実在するファイルパス（例えばUnix上の絶対パス）をそのまま位置引数として渡しても、オプションとして誤解釈されずファイルパスとして扱われる（`split_option`が実在パスを優先する仕様の確認）
- [ ] 保存済みの `.WinMerge` プロジェクトファイルを唯一の位置引数として `winxmerge project.WinMerge` を実行すると、9章で確認した内容で開く。2つ目の引数を追加すると通常の2-wayとして扱われる（プロジェクトファイルとして解釈されない）

---

## 15. 外部連携（Git difftool / mergetool、macOS Finder、プラグイン）

Gitからの呼び出し、macOS Finderの右クリック連携、外部コマンドプラグインが、単一インスタンス上でタブとして正しく積み上がることを確認する。

- [ ] `cargo build --release --features desktop` でビルドしたバイナリをPATHに置き、`git config --global diff.tool winxmerge` / `git config --global difftool.winxmerge.cmd 'winxmerge "$LOCAL" "$REMOTE"'` / `git config --global difftool.prompt false` を設定する。適当なリポジトリで変更後に `git difftool` を実行すると winxmerge が2-way比較で起動する
- [ ] 同様に `git config --global merge.tool winxmerge` / `git config --global mergetool.winxmerge.cmd 'winxmerge "$BASE" "$LOCAL" "$REMOTE"'` / `git config --global mergetool.winxmerge.trustExitCode true` を設定し、コンフリクトのあるマージで `git mergetool` を実行すると3-way表示で起動する。保存して終了するとGit側がマージ結果として取り込む
- [ ] winxmergeを1つ起動したまま、複数ファイルの変更がある状態で `git difftool` を実行する。**期待結果: 新しいウィンドウが増えるのではなく、既存ウィンドウに新しいタブとしてファイルペアが追加される**（IPC・Unixドメインソケットによるシングルインスタンス検出）
- [ ] （Windows）Unixドメインソケットを使わない環境でも上記のシングルインスタンス連携が同様に機能する、または少なくとも複数ウィンドウが乱立せずに動作することを確認する
- [ ] `git difftool` で3ペア以上のファイルが一度に渡された場合、個別のタブではなく仮想フォルダ比較ビューとして一覧表示され、一覧内のファイルをダブルクリックすると詳細な2-way差分が新しいタブで開く
- [ ] （macOS/Linux）`--server` オプションでIPCサーバーとして起動した状態で、別プロセスから `winxmerge dirA dirB`（フォルダ×フォルダ）を渡す。新しいタブでフォルダ比較が開くこと（クラッシュしたり無応答のまま終了しないこと）を確認する
- [ ] IPC経由でファイルペアを渡した比較に対して、送信元プロセスで指定した `/m` や `/ignorews` などのCLIオプションが引き継がれるか確認する。**期待結果: パス以外のオプションはIPC経由では転送されず、受信側インスタンスの既存設定がそのまま使われる**
- [ ] （macOS）`./scripts/build-macos-bundle.sh` でビルドした `WinXMerge.app` を `/Applications` に配置し、システム設定 → 一般 → ログイン項目と機能拡張 でFinder Sync Extensionを有効化する
- [ ] （macOS）Finderで2つのファイルを選択して右クリック → Compare with WinXMerge を実行すると2-way比較ビューが開く
- [ ] （macOS）Finderで3つのファイルを選択して右クリック → Compare with WinXMerge を実行すると3-way比較ビューが開く
- [ ] （macOS）1つのファイルを右クリック→Mark for Compareした後、別のファイルを右クリック→「Compare with [マークしたファイル名]」を実行すると、その2ファイルの比較が開く
- [ ] （macOS）winxmergeが既に起動している状態でFinder拡張から比較を実行すると、新しいインスタンスではなく既存ウィンドウに新規タブとして追加される
- [ ] `~/.config/winxmerge/settings.json` の `plugins` 配列に `{"name": "...", "command": "echo {LEFT} {RIGHT}"}` を追加してアプリを再起動すると、Plugins メニューにその名前の項目が表示される。クリックすると `{LEFT}` `{RIGHT}` が現在のタブの実パスに置換されてコマンドが実行される
- [ ] `plugins` 配列が空、またはsettings.jsonにplugins自体が存在しない状態で起動すると、Plugins メニューに "(No plugins configured)" が表示され、クリックしても何も起きない

---

## 16. Web 版（WASM）

WASMビルドが `cargo build --features desktop` を前提とするデスクトップ専用機能（tree-sitterハイライト、rfd、arboardなど）なしで正しく動作することを確認する。

- [ ] `trunk build`（または `trunk serve`）でWASM版をビルドし、成功する（`--features desktop` を付けていないことを確認する — desktopを付けるとバイナリターゲットのビルドが要求されエラーになる）
- [ ] ビルドしたWeb版をブラウザで開き、ファイル選択UI（`<input type="file">` ベース）から2つのファイルを選んで2-way比較ができる
- [ ] Web版で3-way比較（3ファイル選択）ができる
- [ ] Web版でCSV/TSVのテーブル比較ができる
- [ ] Web版で `.rs` など対応言語のファイルを開いても、シンタックスハイライトが**適用されない**ことを確認する（tree-sitter関連クレートはデスクトップ限定の依存関係のため）
- [ ] Web版でクリップボード連携（Use Clipboard as Left/Right）や外部エディタで開く機能が、メニュー上に出ない、またはクリックしても何も起きないことを確認する（`arboard`/`open`クレートはデスクトップ限定）
- [ ] Web版でファイルダイアログを介さないネイティブなフォルダ選択（フォルダ比較）が利用できない、または代替のUIになっていることを確認する
- [ ] Web版のPlugins機能について、外部コマンド実行が行われない（ブラウザのサンドボックスでは実行不可能なため、メニュー自体が出ないか実行しても失敗する）ことを確認する
- [ ] Web版でウィンドウを閉じる・タブを閉じる際の未保存確認が、ブラウザのタブ/ウィンドウを閉じる操作に対して機能する（`beforeunload`相当の警告が出る、またはWASM側で保存済みかどうかの状態が正しく管理されている）ことを確認する
- [ ] デスクトップ版とWeb版で同じテストデータ（`testdata/left.txt`/`right.txt`）を比較し、差分の検出結果（差分行・件数）が一致することを確認する

---

## 17. リリース手順

タグ付けからGitHub Releaseの資産公開・リリースノート確定までを、実際に手を動かして最後まで通すことを確認する。ここが抜けると、ビルドは通っていても配布物が壊れている、あるいはリリースノートが空のまま公開される。

- [ ] リリース対象のコミットSHAに対して `git tag -s vX.Y.Z -m "vX.Y.Z" <sha>` で署名付きタグを作成する（`-s`を忘れると署名なしタグになるため、`git tag -v vX.Y.Z` で署名を確認する）
- [ ] 作成したタグを `git push origin vX.Y.Z` でpushし、`.github/workflows/release.yml` の Release ワークフローが起動する（トリガーは `v*` タグのpush）
- [ ] ワークフローの3つのビルドジョブ（linux+windowsのマトリクス、macOSの単独ジョブ）と、最後の`release`ジョブがすべて成功で完了する
- [ ] 生成されたGitHub ReleaseページにErrorや失敗マークがなく、Draftではなく公開状態になっている
- [ ] Releaseページの Assets に次の3つがすべて存在し、それぞれ0バイトでないことを確認する: `winxmerge-linux-x86_64.tar.gz` / `winxmerge-macos-aarch64.zip` / `winxmerge-windows-x86_64.zip`
- [ ] linux用tar.gzを展開して `winxmerge` バイナリが実行可能であること、windows用zipを展開した `winxmerge.exe` がWindows（または対応する互換レイヤー）で起動すること、macOS用zipを展開した `WinXMerge.app` が（Gatekeeperの警告が出る場合は正しい手順で）起動することを、可能な範囲でそれぞれ確認する
- [ ] **公開されたリリースノートの本文を実際に読む。** `release.yml` は `generate_release_notes: true` のみを設定しており明示的な `body:` を持たないため、本文はGitHubの自動生成コミット一覧だけになっている。今回のリリースに謝辞・既知の問題・移行手順など追記すべき内容がないかを判断し、あれば `gh release edit vX.Y.Z --notes-file <file>` などで追記する。**自動生成のみで十分と判断した場合は、その判断をこの作業記録（コピーしたチェックリストの側）に残す**
- [ ] 今回のリリースに外部コントリビューター（プロジェクトのコミッター以外)によるPRや変更が含まれている場合、英語で謝辞をリリースノートに追記する（自動生成部分が英語のため）。含まれていない場合はこの項目に「対象なし」と記録する
- [ ] リリース後、`Cargo.toml` の `version` と `[package.metadata.bundle].version`、`macos/Info.plist` と `macos/FinderSync/Info.plist` の `CFBundleShortVersionString` が、今回付けたタグのバージョンと一致していることを確認する（過去に0.38.0のまま長期間放置されていた実績があるため）
- [ ] リリース後、README.mdのバージョンに関する記述（あれば）やGetting Startedのコマンド例に古いバージョン番号やビルド手順の誤り（`--features desktop` の欠落など）が残っていないか、ついでに目視で確認する

---

## 付記: このチェックリストでは確認できない・準備できなかった項目

- 準備章で用意する `.xls`（旧Excel形式）・`.ods`（OpenDocument）のサンプルファイルは `testdata/` に存在せず、社内に既存のサンプルもない場合は用意できない。その場合は該当項目をスキップし、スキップした旨を結果に明記する。
- Windows実機でのIPC/シングルインスタンス連携の詳細（Unixドメインソケットを使わない場合の代替実装の有無）は、コード上の根拠が薄いため、実機で確認した結果をそのまま事実として記録する。
