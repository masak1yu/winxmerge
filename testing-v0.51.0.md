# testing-v0.51.0.md — v0.51.0 リリース前手動テスト（重要章のみ）

対象コミット: `9452e9b`（feature/v0.51.0）
方針: 高リスク章（3 / 10 / 12 / 17）のみ実行。残り165項目は「未実施（次リリースで実行）」として記録する。
fail が出たら項目のチェックは付けず、下の「fail 記録」に症状を書く。

## 事前に済んでいる項目（headless 実行済み）

- [x] `cargo build --features desktop` 警告ゼロ
- [x] `cargo test --features desktop` 84 passed / 0 failed
- [x] `cargo fmt --check` 差分なし
- [x] clippy bin 128 / bin(test) 132（main と同値、増分ゼロ）
- [x] `/?` `--help` `-h` が USAGE を stdout に出し exit=0 で終了
- [x] `trunk build` 成功（trunk 0.21.14）

---

## 3. ファイル比較（2-way）

2ファイルのテキスト差分表示・ナビゲーション・コピー系操作が、単純な追加/削除/変更のそれぞれで期待どおり動くことを確認する。

- [x] `testdata/left.txt` と `testdata/right.txt` を開き、差分行が追加=緑・削除=赤・変更=黄など色分けされて表示される
- [ ] View → Word Diff をONにすると、変更行の中で実際に異なる単語だけが強調表示され、行全体ではなく単語単位のハイライトになる
- [ ] View → Diff Only をONにすると、差分のない行が畳まれて非表示になり、差分のある行の前後だけが表示される
- [x] View → Diff Detail Pane をONにすると、現在選択中の差分の詳細（変更前後のテキストなど）を表示する専用ペインが開く
- [ ] ~~View → Wrap Lines をONにすると、長い行がペイン幅で折り返して表示され、水平スクロールバーが消える~~ → F5 の修正で項目ごと削除（折り返しは #77）
- [x] Merge → First Difference / Previous Difference / Next Difference / Last Difference をそれぞれクリックし、現在の差分がその通りに移動する（3件以上差分があるファイルペアで確認する）
- [ ] Alt+↓（次）/ Alt+↑（前）/ Alt+Home（最初）/ Alt+End（最後）のキー操作でも同様に差分間を移動できる
- [x] ツールバーの先頭/前/次/末尾ボタン（Merge menuと同じ4操作）でも同様に移動できる
- [x] Merge → Copy Left to Right を実行すると、現在の差分の左側の内容が右側にコピーされ、その行が差分でなくなる
- [x] Merge → Copy Right to Left でも逆方向に同様の結果になる
- [x] ツールバーの「コピー＆次へ」ボタン（→側／←側）を押すと、コピー実行後に自動で次の差分に移動する
- [x] ツールバーの「全差分をコピー」ボタン（左→右、右→左それぞれ）を押すと、そのファイルペアの全差分が一括でコピーされ、以降の比較で差分が0件になる
- [x] Edit → Copy All Left Text / Copy All Right Text を実行し、それぞれ左ファイル全体・右ファイル全体のテキストがクリップボードにコピーされる（別のテキストエディタに貼り付けて確認する）
- [x] 左ペインでテキストを選択した状態でEdit → Copy Selection →を実行すると、選択範囲だけが右ペインの対応する位置にコピーされる。← Copy Selectionでも逆方向に同様に動く
- [x] クリップボードに任意のテキストをコピーした状態でEdit → Use Clipboard as Left を実行すると、左ペインの内容がクリップボードの内容に置き換わり、その内容と右ファイルとの比較になる。Use Clipboard as Right でも同様
- [x] 拡張子が `.rs` など対応言語のファイルペアを開くと、キーワードや文字列リテラルなどがシンタックスハイライトされる。デスクトップ版でのみ有効な機能であることを確認する（WASM版では色分けされない — 16章参照）
- [ ] 片方のファイルで数行を上下に入れ替えた（移動した）テキストペアを比較し、移動元・移動先の行が青系のハイライトで示される。フォルダ比較のリストと違い、この移動行検出はトグルできるオプションである（12章参照）

---

## 10. インライン編集と保存

差分ペイン内でのテキスト編集がゴースト行の整合性を保ったまま行え、Rescanしても編集内容が失われないことを確認する。あわせて、Issue #63（比較時に無視した行が保存で消える）に対する v0.51 の暫定対応——行が落ちうる設定では保存そのものをブロックする——が意図どおり効いていることを確認する。

- [x] 2-way比較のいずれかのペインで行頭にカーソルを置きEnterを押すと、その位置に新しい行が挿入され、反対側のペインには整列を保つためのゴースト行が自動的に追加される
- [x] ゴースト行に対して直接テキストを入力すると、そのゴースト行は通常の行として扱われるようになる（is_ghostがfalseになり、以後の保存対象に含まれる）
- [x] 行頭にカーソルがある空行でBackspaceを押すと、その空行が削除される
- [x] Arrow Up / Down でペイン内の行間をカーソル移動できる
- [x] 実在するファイルの内容と異なるファイル（外部で書き換えた）を、未保存の編集がある状態のタブで開いたままにしておく。**期待結果: 自動再スキャンはファイルの再読み込みをスキップし、編集内容を上書きしない**
- [x] 上記と同じ状態でF5を手動で押すと、現在エディタに表示されている（未保存の）編集内容から差分が再計算される（ディスク上のファイルではなく、画面上の内容が基準になる）
- [x] 1行しか残っていない状態のペインで、その最後の行を削除する操作を行う。**期待結果: 削除は無効化され、そのペインが編集不能な空の状態にならない**
- [x] Cmd/Ctrl+Zで直前の編集を取り消し、Cmd/Ctrl+Shift+Zでやり直せる。取り消し・やり直しを連続で複数回実行しても内容が破綻しない
- [x] File → New(N) → Text で作った空の2-way文書に数行入力し、Save Asで保存すると、新規作成した内容がそのままファイルに書き出される
- [x] Edit → Options... → Filters で「空行を無視」をONにする。行数の異なる空行を含む2つのテキストファイル（例: 左に空行が1行多い `testdata/left.txt` の複製を作り空行を足したもの）を開いて2-way比較し、Cmd/Ctrl+Sで左ファイルを保存しようとする。**期待結果: 保存は実行されず、ステータスバーに `Save blocked: turn off "Ignore blank lines" and clear line filters first ...(#63)` が表示される。** 保存しようとしたファイルをテキストエディタで開き、ディスク上の内容が一切変わっていない（無視された空行がそのまま残っている）ことと、未保存マークが消えていないことを確認する
- [x] 行フィルタ（Options → Filters の行フィルタ欄）に何か入力した状態でも、同じくCmd/Ctrl+Sでの保存がブロックされ、同じメッセージが出ることを確認する
- [ ] 続けて「空行を無視」をOFFに戻し行フィルタ欄も空にしてから、もう一度Cmd/Ctrl+Sを押す。**期待結果（F9 で変更）: ON の間に比較したタブは、OFF に戻しても保存がブロックされたまま。ファイルを開き直すと保存が通り、空行を含む元の内容がそのまま書き出される**（ON の間に行が落ちた PaneBuffer から保存しないための確認）
- [x] 「空行を無視」をONにしたまま未保存の編集があるタブを開き、アプリを終了する。**期待結果（F13 で変更）: 終了は取り消され、タブと未保存の編集が残る。ステータスバーに `Quit cancelled — save blocked for "<タブ名>": ...` が表示され、ディスク上の元ファイルから行は消えていない。** 根本修正は v0.52（#63）
- [x] 上記と同じ「空行を無視」設定のまま、`testdata/base.txt` / `testdata/left_3way.txt` / `testdata/right_3way.txt` で3-way比較を開く。**期待結果: 「空行を無視」の設定は3-way比較には一切反映されず、空行を含めた通常の差分・コンフリクト判定になる**（2-wayの設定値を共有するUIのチェック状態がONのままでも、3-wayの比較結果には影響しない）

---

## 12. フィルタと差分オプション

行フィルタ・置換フィルタと5種類の差分オプションが、ツールバー・メニュー・Optionsダイアログ・CLIのどこから到達できるかを含めて確認する。ここには保存がブロックされる暫定対応（Issue #63）と、3-wayでの非対応範囲を含む。

- [x] Edit → Options... → Filters の行フィルタ欄に `TODO|FIXME` のようなパイプ区切りの正規表現を入力してApplyすると、その正規表現にマッチする行が差分計算から除外される（該当行が差分としてもEqualとしても表示されず、行自体が比較対象から消える）
- [x] Filters の置換フィルタ（Find/Replaceのペア）を1つ追加してApplyすると、指定した正規表現にマッチする箇所が比較時だけ置換後の文字列として扱われ、差分が変わる。保存後のファイルの中身は元のテキストのままで、置換フィルタの影響を受けていないことを確認する（行フィルタ欄は空にし「空行を無視」もOFFにしてから試す。どちらかが有効だと10章のガードで保存自体がブロックされるため）
- [x] ツールバーを見て、Ignore Whitespace・Ignore Caseの2つのトグルボタンだけが存在することを確認する。**期待結果: 「空行を無視」「改行コードを無視」「移動行の検出」の3つはツールバー上にボタンがなく、Edit → Options...またはView/Merge相当のメニューからのみ切り替えられる**（ドキュメントはツールバーに5種類あると書いているが実際は2種類のみ）
- [ ] View → Ignore Whitespace / Ignore Case のチェック状態が、ツールバーの同名ボタンの状態と常に一致している（どちらから切り替えても他方に反映される）
- [x] `/ignorews:1`（空白の変更を無視）と `/ignorews:2`（空白をすべて無視）をそれぞれ指定して起動し、Options上のWhitespaceの設定が「Ignore whitespace change」「Ignore all whitespace」のそれぞれ正しいモードになっていることを確認する
- [x] Ignore Blank Lines をONにして起動し、行の追加/削除だけでなく空行の有無が異なるファイルペアで、空行の違いが差分として表示されなくなることを確認する
- [x] Ignore EOL（改行コードを無視）をONにして、改行コードだけが異なる（CRLF/LF）2つのファイルを比較すると差分なしになる
- [ ] 移動行検出のトグルをOFFにすると、3章で確認した移動行の青ハイライトが出なくなり、単純な追加+削除として表示される
- [x] ここまでのフィルタ・差分オプションの設定値が、アプリを再起動しても保持されている（settings.jsonに保存されるため）
- [x] 10章の該当項目（空行無視・行フィルタが有効なとき2-wayテキストの保存がブロックされる件。Issue #63 の暫定対応）と、3-wayでは空行無視・行フィルタが完全に無効である件を、本章の観点からも再確認する（後者は既知の制限として結果に記録する）

---

## 17. リリース手順

タグ付けからGitHub Releaseの資産公開・リリースノート確定までを、実際に手を動かして最後まで通すことを確認する。ここが抜けると、ビルドは通っていても配布物が壊れている、あるいはリリースノートが空のまま公開される。

- [ ] リリース対象のコミットSHAに対して `git tag -s vX.Y.Z -m "vX.Y.Z" <sha>` で署名付きタグを作成する（`-s`を忘れると署名なしタグになるため、`git tag -v vX.Y.Z` で署名を確認する）
- [ ] 作成したタグを `git push origin vX.Y.Z` でpushし、`.github/workflows/release.yml` の Release ワークフローが起動する（トリガーは `v*` タグのpush）
- [ ] ワークフローの3つのビルドジョブ（linux+windowsのマトリクス、macOSの単独ジョブ）と、最後の`release`ジョブがすべて成功で完了する
- [ ] 生成されたGitHub ReleaseページにErrorや失敗マークがなく、Draftではなく公開状態になっている
- [ ] Releaseページの Assets に次の3つがすべて存在し、それぞれ0バイトでないことを確認する: `winxmerge-linux-x86_64.tar.gz` / `winxmerge-macos-aarch64.zip` / `winxmerge-windows-x86_64.zip`
- [ ] linux用tar.gzを展開して `winxmerge` バイナリが実行可能であること、windows用zipを展開した `winxmerge.exe` がWindows（または対応する互換レイヤー）で起動すること、macOS用zipを展開した `WinXMerge.app` が（Gatekeeperの警告が出る場合は正しい手順で）起動することを、可能な範囲でそれぞれ確認する
- [ ] **公開されたリリースノートの本文を実際に読む。** `release.yml` は `generate_release_notes: true` のみを設定しており明示的な `body:` を持たないため、本文はGitHubの自動生成コミット一覧だけになっている。今回のリリースに謝辞・既知の問題・移行手順など追記すべき内容がないかを判断し、あれば `gh release edit vX.Y.Z --notes-file <file>` などで追記する。**自動生成のみで十分と判断した場合は、その判断をこの作業記録（コピーしたチェックリストの側）に残す**
- [ ] 今回のリリースに外部コントリビューター（プロジェクトのコミッター以外)によるPRや変更が含まれている場合、英語で謝辞をリリースノートに追記する（自動生成部分が英語のため）。含まれていない場合はこの項目に「対象なし」と記録する
- [ ] リリース後、`Cargo.toml` の `version` と `[package.metadata.bundle].version`、`macos/Info.plist` と `macos/FinderSync/Info.plist` の `CFBundleShortVersionString` が、今回付けたタグのバージョンと一致していることを確認する（過去に0.38.0のまま長期間放置されていた実績があるため）
- [ ] リリース後、README.mdのバージョンに関する記述（あれば）やGetting Startedのコマンド例に古いバージョン番号やビルド手順の誤り（`--features desktop` の欠落など）が残っていないか、ついでに目視で確認する

---


## fail 記録

（項目番号 / 症状 / 再現手順 を書く。ここに書かれたものを私が修正する）

### F1. 共通キーボードショートカットがデスクトップ版で届かない（3章 L28 / 10章 L53・L55-57 に波及）

- 症状: `left.txt` / `right.txt` を開いた直後に Alt+↓ / Alt+End を押しても「Difference 1 of 3」のまま。Cmd+T でタブが増えない。ペインの行をクリックして TextInput にフォーカスがある状態でも Alt+↓ で差分が移動しない
- 再現手順: `winxmerge testdata/left.txt testdata/right.txt` で起動 → そのまま Alt+↓（またはペインをクリックしてから Alt+↓）
- 根拠:
  - ショートカットを受ける `FocusScope` は `width: 0; height: 0` で子要素を持たず、ペインの祖先でもない（`ui/main.slint:2053-2133`）。v0.3.0 で追加されたとき（`d3e17eb:ui/main.slint:471-473`）から同じ形
  - root に `forward-focus` が無く、`focus()` を呼ぶのは Save As ダイアログだけ
  - Slint 1.15.1 はキーイベントを「フォーカス中の要素とその親」にしか配送しない。フォーカス要素が無ければ誰にも届かない（`i-slint-core-1.15.1/window.rs:784-827` `process_key_input`）
- 影響（推論）: `ui/main.slint:2058-2130` の全ショートカット（Cmd/Ctrl+S/F/Z/T/W/N/G/M、Alt+矢印、F2、F5、Esc）。TextInput フォーカス中の Cmd+Z は Slint の TextInput 組み込み undo が効いているだけの可能性がある

### F2. 移動行を含むファイルペアを開くとクラッシュする（3章 L38 / 12章 L74）

- 症状: 起動直後にプロセスが終了する。2回起動して2回とも再現
- 再現手順: 左 `alpha bravo charlie delta echo foxtrot golf hotel`、右 `alpha bravo foxtrot golf charlie delta echo hotel`（1行1語）を比較する。移動行検出（`detect_moved_lines`）は既定で ON（`src/settings.rs:193`）
- ログ: `panicked at src/app/pane_buffer.rs:182:49: called Option::unwrap() on a None value`。バックトレースは `build_pane_buffers_2way` ← `apply_diff_result` ← `recompute_diff_from_text_with_highlights` ← `run_diff` ← `start_compare`
- 根拠: `src/diff/engine.rs:303-304` は「削除行」と「追加行」を両方 `LineStatus::Moved` に書き換える。削除行は `right_line_no` を持たず、追加行は `left_line_no` を持たない。ところが `pane_buffer.rs:180-183` は `Moved` を `Modified` と同じ分岐で扱い、両方の行番号を `unwrap()` している
- 混入: `3a9921a`（v0.42.0、PaneBuffer 導入）。このブランチの変更は pane_buffer.rs のテスト追加だけで無関係

### F3. 「差分のみ表示」で畳まれた行の文字が他の行に重なって描画される（3章 L24）

- 症状: 左ペインで `Another common line.` と `The end.` が同じ位置に重なり、行番号も 2/3、5/6 が重なる。AX ツリーでも畳まれたはずの行の TextField が高さ 15 のまま隣の行と同じ y 座標にある
- 再現手順: `left.txt` / `right.txt` を開き、表示 → 差分のみ表示
- 根拠（推論を含む）: 行の高さは 0px にしている（`ui/widgets/diff-view.slint:375, 430`）が、行内の TextInput は `height: (text-font-size + 2) * 1px` 固定（同 153）で、クリップもしていない。そのため高さ 0 の行からはみ出して描画されていると考えられる

### F4. メインペインにワード単位の差分強調が出ない（3章 L23）

- 症状: 変更行（`This is a test file.` ↔ `This is a modified test file.`）でワード差分を ON/OFF しても、メインペインの描画が画素レベルで変わらない（拡大キャプチャで比較）。下の詳細ペインは ON でも OFF でも `modified` が強調されたままで、トグルがどこにも効いていない
- 根拠: 行コンポーネントは `word-diff` プロパティを受け取っている（`ui/widgets/diff-view.slint:38, 389, 444`）が、行内のどこからも参照されていない。詳細ペイン側がトグルを見ていない理由は未調査

### F6（仕様確認）. 行頭で Enter を押しても、その行の上ではなく下に空行が入る（10章 L46）

- 症状: `Hello, World!` の行頭で Enter を押すと、空行は `Hello, World!` の下（2行目）に入り、`Hello, World!` は1行目に残る。反対側にはゴースト行が同じ位置に入る（ここは期待どおり）
- 根拠: TextInput の `accepted` は `insert-line-after(idx)` を呼ぶだけで、カーソル位置を見ていない（`ui/widgets/diff-view.slint:160, 398`）。行を分割しないのは実装どおりの挙動なので、バグとして直すか、テスト項目の期待結果を「下に空行が入る」に書き換えるかは要判断

### F5. 「行を右端で折り返す」が ON でも長い行が折り返されない（3章 L26）

- 症状: 60語の長い行を含むファイルペアで ON/OFF を切り替えても、どちらも行がペイン幅で切れて水平スクロールバーが残る
- 根拠（推論を含む）: `wrap` を設定しているのは編集不可のときの `Text` だけ（`ui/widgets/diff-view.slint:146`）。インライン編集が有効なときに使う `TextInput`（同 151-178）には `wrap` が無い。さらに行の高さは1行分で固定されている（同 377）ので、折り返しても2行目以降を表示する余地が無い

### F7（仕様確認）. ゴースト行をクリックしても入力できない（10章 L47）

- 症状: `left.txt` / `right.txt` で右ペインのゴースト行（左2行目の反対側）をクリックして `ghost` と打つと、ゴースト行は空のまま。文字は直前にフォーカスがあった左ペインの行に入った
- 根拠: 行の `editable` は `root.inline-edit-enabled && !line.is-ghost`（`ui/widgets/diff-view.slint:390, 445`）なので、ゴースト行は TextInput ではなく Text で描画され、フォーカスを取れない。テスト項目は「ゴースト行に直接入力すると通常の行になる」と書いているが、実装はゴースト行を編集対象から外している。Gotchas 1 の「ghost→通常行」への変換はコピー操作の経路の話。バグとして直すか、テスト項目を書き換えるかは要判断

### F8. 2-way で行頭 Backspace による行削除が、反対側の実在行まで消す。最後の1行も消せてしまう（10章 L52）

- 症状1（最後の行）: `one_l.txt`（`only line`）/ `one_r.txt`（`other line`）で左の行を Cmd+A → Backspace で空にし、もう一度 Backspace を押すと、左右どちらのペインも行が0になる。ペインをクリックして文字を打っても何も入らない（ステータスは `Editing — press F5 to compare` のまま）
- 症状2（途中の行、ディスク上のデータ消失）: `left.txt` / `right.txt` のコピーで、左2行目（`This is a test file.`、右の `This is a modified test file.` と対になる変更行）を Cmd+A → Backspace で空にし、もう一度 Backspace を押す。すると右ペインの `This is a modified test file.` も消える。右は一度も編集していないのに、ファイル → 右を保存 で `Right file saved` になり、ディスク上の右ファイルからその行が消えた（`diff` で `2d1 < This is a modified test file.` を確認）
- 根拠: 2-way の `delete_line`（`src/app/text_edit.rs:387-435`）は、行が空かどうかだけを見て、左右両方の PaneBuffer から同じ index の行を消す（416-421）。残りの行数のガードは無く（Gotchas 2 は `three_way_delete_line` 側の対策）、反対側の行がゴーストかどうかも見ていない

### F9. 「空行を無視」ON の間に編集し、OFF に戻して保存すると、空行がディスクから消える（10章 L57。#63 の暫定ガードをすり抜ける）

- 症状: `blank_l.txt`（`left.txt` の3行目の前に空行を足したもの）/ `blank_r.txt`（`right.txt` の複製）で、次の順に操作する。Options の「空行を無視」を ON → 適用 → 左1行目を編集 → 「空行を無視」を OFF → 適用 → ファイル → 左を保存。保存は通るが、ディスク上の左ファイルから空行が消える（`diff` で `3d2 <（空行）`）。10章 L55-57 を書かれた順に進めると、この状態になる
- 対照: ON → 適用 → 編集せずに OFF → 適用 → 編集 → 保存、の順なら空行は残る（ペインに空行が戻り、`diff` の差分は編集した1行だけ）
- 根拠: ON で適用した時点で、左ペインから空行の行が消える（AX ツリーで確認）。OFF で適用すると、`apply_options` は編集中（`editing_dirty || has_unsaved_changes`）なら `rescan()` を呼ぶ（`src/app/options.rs:115-118`）。`rescan()` はディスクではなく PaneBuffer から再構築する（`src/app/diff_text.rs:443`）ので、消えた空行は戻らない。保存ガード（`src/app/save_export.rs:535`）は保存する瞬間の設定しか見ていないので、OFF に戻した後の保存を止められない
- 影響: #63 の暫定対応（`d743eb3`）は「トグルを戻せばブロックが解除される、可逆な仕組み」を前提にしているが、その前提が崩れる。暫定対応の範囲で塞ぐ案（推論）: ON の間に編集されたタブを記録しておき、OFF に戻した後も保存をブロックし続ける

### F10. メニューの「Quit winxmerge」を使うと、未保存の編集があっても確認なしで終了する（10章 L58）

- 症状: `blank_l.txt` のコピーを編集して未保存（`● 変更あり`）にしたまま、メニュー winxmerge → Quit winxmerge を選ぶ。するとダイアログを出さずに、2秒以内にプロセスが終了した。編集は失われる（ディスク上のファイルは変わらない）。一方、ウィンドウの閉じるボタンで閉じた場合は「未保存の変更があります。どうしますか？」ダイアログ（保存(S) / 保存しない(D) / キャンセル）が出る
- 根拠（推論を含む）: 未保存チェックは `window.window().on_close_requested`（`src/main.rs:2386`）にしか無い。「Quit winxmerge」は macOS 標準のアプリメニューの項目で、ウィンドウの close request を経由せずにアプリを終了させていると考えられる（未検証）。Cmd+Q も同じ経路を通ると考えられるが、未確認

### F11. 「改行コードの違いを無視」が OFF でも、CRLF と LF の違いが差分にならない（12章 L73）

- 症状: CRLF だけの `crlf_l.txt` と、同じ内容を LF にした `crlf_r.txt` を比較すると、`/ignoreeol:1` でも `/ignoreeol:0` でも `Files are identical` になる
- 根拠: 比較前の正規化 `normalize_text` は `text.lines()` で行を分けている（`src/diff/engine.rs:351`）。`str::lines()` は `\r\n` の `\r` も取り除くので、`ignore_eol` が false でも比較する文字列に `\r` が残らない。`ignore_eol` が true のときの `trim_end_matches(['\r', '\n'])`（同 352-353）は、落とすものが無い
- 影響: 改行コードの違いは、設定にかかわらず差分に出ない。L73 の手順（無視を ON にすると差分なしになる）は OFF でも同じ結果になるので、確認にならない

### F12. CRLF だけのファイルの改行コードが、ステータスバーで `Mixed` と表示される（12章 L73 で発覚）

- 症状: `file` コマンドで `with CRLF line terminators` と判定されるファイルを開くと、ステータスバーに `Mixed | LF` と出る（右は LF のファイル）
- 根拠: `detect_eol`（`src/encoding.rs:54-73`）は `has_lf` を `check.contains(&b'\n')` で求めている（61）。`\r\n` の中の `\n` も数えるので、CRLF のファイルでは常に true になる。`"CRLF"` を返す条件は `has_crlf && !has_cr_only && !has_lf`（62）なので成立せず、`Mixed` になる（64-65）
- 未調査: 保存時に、この判定が書き出す改行コードに影響するか

### F13. 終了時に保存がブロックされても、`Save blocked for ...` が見えないまま終了する（10章 L58）

- 症状: 「空行を無視」ON で未保存の編集があるタブを開いたまま、ウィンドウの閉じるボタン → 「保存(S)」を押すと、保存されずに終了する（ここは期待どおり）。ただし期待結果にある `Save blocked for "<タブ名>": ...` は、ステータスバーから読めなかった。クリックと並行してステータスバーの文字列を読み続けたが、この文字列は一度も出ず、2回目の読み取りより前にプロセスが終了していた
- 根拠: 「保存(S)」の `quit-save-all` は `collect_pending_saves` を呼び、すぐ後に `advance()` を呼ぶ（`src/main.rs:2493-2498`）。ブロックされたタブはメッセージを設定するだけで、保存キューに入らない（`src/app/save_export.rs:87-95`）。キューが空だと、`advance()` はその場で `slint::quit_event_loop()` を呼ぶ（`src/main.rs:2463-2467`）。メッセージの設定と終了が同じコールバックの中で起きるので、画面が描き直される前に終了すると考えられる（推論。描画のタイミングは未検証）
- 影響: 編集が保存されずに失われたことを、ユーザーが知る手段が無い。L58 の期待結果は、このメッセージが表示されることを前提にしている

### F14. タブを閉じるときの「保存」がブロックされても、編集を捨ててタブを閉じる（F13 の調査で発覚）

- 症状（コードから。修正前の E2E は未実施）: 「空行を無視」ON で編集したタブを閉じ、未保存ダイアログで「保存」を押すと、保存はブロックされるのにタブが閉じて編集が失われる。ファイルを開く前の「保存して続行」も同じ
- 根拠: `on_tab_close_save`（`src/main.rs:458-486`）は `save_file` の結果を見ずに `has_unsaved_changes = false` にして `force_close_tab` する。`on_save_and_proceed`（同 403-434）も結果を見ずに続行する。`save_file` は結果を返さない（`src/app/save_export.rs:518`）
- 混入: `d743eb3`（このブランチの #63 ガード）で、この呼び出し元を見落とした

## 修正（feature/v0.51.0）

- F1: `2459133`、`ae19098`（undo / redo / F5 の後もキーが届く）
- F2: `0e44226`
- F3: `0e2e731`
- F4: `a1b13cf`（詳細ペインがトグルに従う。メインペインの単語ハイライトは未実装）
- F5: `bd8d511`（メニュー項目と Options のチェックを削除。折り返しは #77）
- F6: `ba386cb`（Enter でカーソル位置から行を分割）
- F7: `afb8780`（ゴースト行に入力すると通常の行になる）
- F8: `b72ddfc`
- F9: `2c46530`（ON の間に行が落ちたタブは、OFF に戻してもファイルを読み直すまで保存をブロック）
- F10: `7821cd4`
- F11: `493f07b`
- F12: `26330f4`
- F13: `8815bae`（ブロックされたタブがあれば終了を取り消す。L58 の期待結果が変わる）
- F14: `54768c5`

---

## 実行状況（2026-09-14 時点）

GUI の操作は、`9452e9b` のデバッグビルド（`target/debug/winxmerge`）を macOS 上で osascript / CGEvent から動かして行った。テストデータは `testdata/` のコピーを使った。12章 L67 以降と 10章 L54 / L58 は画面キャプチャを使わず、アクセシビリティ API で読める文字列・ステータスバー・ディスク上のファイル・settings.json だけで判定した。

pass（チェック済み）の補足:
- 3章 L34: クリップボードに入る内容には、ファイル末尾の改行が付かない
- 3章 L35: 範囲の選択は、行番号をクリックして行単位で選ぶ方式。両方向とも `Copied 1 lines` になった
- 3章 L37: 色は1行につき1色で、トークン単位の色分けではない
- 10章 L50: 自動再スキャンは既定で OFF（`src/settings.rs:207`）なので、Options で ON にしてから確認した。未編集のタブは外部の変更を読み込み（`Files rescanned`）、編集中のタブは読み込まずに編集内容を残した
- 10章 L59: 3-way（base は `a b c`、左は空行を1行追加、右は `d` を追加）で比較した。「空行を無視」が ON のときも `/ignoreblanklines:0` のときも、`2 differences found` になった
- 12章 L69: ツールバーにある比較オプションのトグルは、W≠（空白を無視）と Aa（大文字小文字を無視）の2つだけ
- 12章 L71: `/ignorews:1` では「空白の違いを無視」が ON で「Ignore all whitespace」が OFF。`/ignorews:2` では両方 ON
- 12章 L72: `blank_l.txt` / `blank_r.txt` で比較した。ON（設定を ON にして起動）だと `+1 -1 ~1`、`/ignoreblanklines:0` だと `+1 -2 ~1`
- 12章 L67: `filt_l.txt`（alpha / TODO fix this / beta）と `filt_r.txt`（alpha / beta / FIXME later）で `TODO|FIXME` を適用した。両ペインから該当行が消え（左の行番号は 1, 3 のまま）、`+0 -0 ~0` になった。settings.json には `["TODO", "FIXME"]` の2要素で保存される
- 12章 L68: `repl_l.txt`（version v1 / same line）と `repl_r.txt`（version v2 / same line）で、置換フィルタ `v[0-9]` → `vN` を適用した。ペインの表示は元の文字列のまま、`+0 -0 ~0` になった。左2行目を `same line!` に編集してファイル → 左を保存 すると保存が通り、ディスク上は `version v1` / `same line!` だった（置換は書き出されない）
- 10章 L54: ファイル → 新規(N) → テキスト で開いた空の文書の左ペインに `alpha` / `beta` / `gamma` と3行打ち、ファイル → 左を保存 を押すと macOS 標準の保存パネルが出た。保存先を変えて保存すると、ステータスが `Left file saved: …/l54_new.txt (UTF-8)` になり、ディスク上の中身は `alpha\nbeta\ngamma\n` だった
- 12章 L75: 再起動の前後で Options の値を比べた。1回目の再起動で行フィルタ `TODO|FIXME` が、2回目の再起動で「大文字小文字を無視」ON・「空行を無視」OFF・「移動した行を検出」OFF・置換フィルタ `v[0-9]` → `vN`・行フィルタが空であることが、それぞれ残っていた

チェックを付けていない項目:
- 3章: L23 → F4、L24 → F3、L26 → F5、L28 → F1、L38 → F2
- 10章 L46 → F6（仕様確認）、L47 → F7（仕様確認）、L52 → F8
- 10章 L51 → F1。F5 キーは届かない。メニューの ファイル → 再スキャン (F5) を使うと、画面上の未保存の編集をもとに差分が再計算された（外部で書き換えた内容は読み込まない）
- 10章 L53 → F1。Cmd+Z / Cmd+Shift+Z は届かない。メニューの 編集 → 元に戻す / やり直し なら、1文字単位で動き、何度繰り返しても内容は崩れなかった
- 10章 L55 / L56 → F1。Cmd+S は届かない。メニューの ファイル → 左を保存 を使うと、保存がブロックされてメッセージが出た。ディスク上のファイルは変わらず、`● 変更あり` も残った
- 10章 L57 → F9（Cmd+S は F1）
- 10章 L58 → F13（メニューの Quit で終了した場合は F10）。「空行を無視」ON で `f58_l.txt`（2行目が空行）/ `f58_r.txt` を開き、左1行目を編集して `● 変更あり` にした。ウィンドウの閉じるボタン → 未保存ダイアログの「保存(S)」を押すと、保存パネルは出ずにプロセスが終了した。ディスク上の `f58_l.txt` はハッシュが編集前と同じで、空行も残っていた（ここまでは期待どおり）。`Save blocked for ...` はステータスバーから読めなかった
- 12章 L74 → 一部のみ。移動行検出を OFF にして `moved_l.txt` / `moved_r.txt`（F2 の再現ペア）を開くと、クラッシュせず `+2 -2 ~0`（単純な追加+削除）になった。青ハイライトが出ないことは、画面キャプチャを使わない方針のため確認していない（目視が必要）。ON のときは F2 で開けないので、ON との比較もできない
- 12章 L70 → 一部のみ。ツールバーの「空白の違いを無視」「大文字小文字の違いを無視」をクリックすると、表示メニューの ✓ が付いたり外れたりした（メニュー項目の名前をアクセシビリティ API で読んで確認）。逆向き（メニューで切り替えたときのツールバーの見た目）は、画面キャプチャを使わない方針のため目視していない。コード上は、メニューの ✓ とツールバーの `active` が同じプロパティに結び付いている（`ui/main.slint:744,748` と `:997,999`。見た目は `:45` の `background`）ので、ずれることはないはず（推論）
- 12章 L76 → F1、F9、F13。保存のブロックそのものは、メニューの ファイル → 左を保存 からは効いた（L55 / L56）。ただし Cmd+S は届かず（F1）、ON の間に編集してから OFF に戻すとガードをすり抜け（F9）、終了時のメッセージは読めなかった（F13）。3-way で「空行を無視」が効かない件は、既知の制限として L59 のとおり（ON でも OFF でも `2 differences found`）
- 12章 L73 → F11、F12

続きの進め方:
- 画面キャプチャ（スクリーンショット）を伴う自動操作は使わない。使用環境のセキュリティ基準に抵触したため
- 自動操作で進められる項目は終わった。残りは目視が要る部分（L70 のツールバーの見た目、L74 の青ハイライト）で、人が画面を見て確認する
- F1〜F13 のうち v0.51 で直す範囲を決める。F1 は Slint のキー配送が原因なので、Linux でも起きると考えられる（推論。Linux で未確認）
- L74 を目視するときは、移動行検出が ON のまま移動行を含むペアを開くと F2 でクラッシュする。先に Options で OFF にしてから開く
- 直す範囲の修正が済んだら: バージョンを 0.51.0 に上げる（`Cargo.toml` の version と `[package.metadata.bundle]`、`macos/Info.plist`、`macos/FinderSync/Info.plist`）→ #63 に暫定対応のコメントを書く（Issue は閉じない）→ PR を作る

## 実行状況（修正後、2026-09-15）

F1〜F14 を直したデバッグビルドで、前回と同じく画面キャプチャを使わずに確認した。

今回チェックした項目:
- 10章 L46 / L47（F6 / F7）: 行頭の Enter で行が分かれ、ゴースト行に打った文字は保存したファイルに入った
- 10章 L51 / L53（F1）: F5 キーと Cmd+Z / Cmd+Shift+Z が届いた。再スキャン・元に戻す・やり直しの後もキーが効いた
- 10章 L52（F8）: 1行だけのペインで行を空にして Backspace を押しても、行は残った。保存すると空行1行になり、反対側のファイルは変わらなかった
- 10章 L55 / L56: 保存のブロックは、前回メニューの ファイル → 左を保存 で確認した。Cmd+S が届くことは、F1 の修正後に別に確認した
- 10章 L58（F13）: 閉じるボタン →「保存(S)」で終了が取り消され、`Quit cancelled — save blocked for "…"` が出た。ディスクは変わらなかった。Cmd+Q とアプリメニューの Quit でも、未保存ダイアログが出た（F10）
- 12章 L73（F11 / F12）: `/ignoreeol:1` で `Files are identical`、`/ignoreeol:0` で差分あり。ステータスバーは `CRLF | LF`
- 12章 L76: F9 / F13 / F14 のブロックを確認した。3-way で空行無視が効かないのは L59 のとおり（既知の制限）

まだチェックしていない項目:
- 3章 L23: メインペインの単語ハイライトは未実装（#76）。詳細ペインは F4 で直したが、目視が必要
- 3章 L24（F3）、L38（移動行の青ハイライト）、12章 L70 / L74: 目視が必要
- 3章 L28: Alt+↓ / Alt+↑ は確認した。Alt+Home / Alt+End は未確認
- 10章 L57（F9）: OFF に戻しても保存がブロックされることは確認した。開き直した後に保存が通ることは未確認

気づき:
- メニューやツールバーから Undo / Redo / 再スキャンした後は、行をクリックするまでキーが効かない（#81）。Options ダイアログやメニューから保存した後も、Cmd+T が効かなかった
- 編集で Redo の履歴が消えても、「やり直し」が有効なまま（#82）
- 終了取り消しの文言が長く、ステータスバーからはみ出すと考えられる（推論。AX の幅 1529px がウィンドウ幅 1200px を超える。見た目は未確認）
- F14 のうち、ファイルを開く前の「保存して続行」の経路は、保存パネルが要るので未確認
